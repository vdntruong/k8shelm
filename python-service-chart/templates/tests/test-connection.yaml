apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-service-chart.fullname" . }}-test-connection"
  labels:
    {{- include "python-service-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-service-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
