apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-service-chart.fullname" . }}
  labels:
    {{- include "go-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-service-chart.selectorLabels" . | nindent 4 }}
